Torn Pulse V2.0.2 link fix

Fixes:
- Item Market 'OPEN IN TORN' no longer fails on Android's canOpenURL pre-check.
- Travel Agency button no longer fails on the same pre-check.
- Baldr's List uses the same direct-link approach for consistency.

Upload App.js to the repository root and replace the existing App.js.
Then commit to main. GitHub Actions will build the APK automatically.
