Torn Pulse V208 — Branding Wire-Up

The branding assets are already in the repository root.

This update activates them:
- app launcher icon -> app-icon-1024.png
- Android adaptive icon -> adaptive-icon-foreground-432.png
- splash screen -> splash-icon-2048.png
- in-app main header -> header-logo-wide-1200x360.png
- settings/sub-header mark -> new TP emblem
- floating HUD -> hud-icon-256.png

Upload BOTH included files to the ROOT of the repo and replace the existing files:
1. App.js
2. app.config.js

Then commit once.
